# TODO — 360° 레이더 테스트 콘솔

> 마지막 갱신: 2026-07-14 (OOB 구현 4단계 완료 — 인수인계 기준).
> 항목을 시작/완료하면 체크박스와 "완료 기록"을 갱신할 것.
> 상세 배경은 각 날짜별 `작업기록_*.md` 참고.

## 0. 현재 상태 (인수인계 요약)

**기준선**: 태그 `v1.0_non-oob` (`b058912`) = OOB 이전 버전. 폰 주소를 **수동 입력**하는
1차 방식으로 실기기 레인징 검증 완료. 이 태그의 동작이 회귀 기준선이다.

**진행 중 작업**: OOB 2차 = BLE로 폰 주소 자동 교환 (§1). 구현 5단계 중 **4단계까지 완료**.

| 단계 | 내용 | 상태 | 검증 수준 |
|---|---|---|---|
| 1 | `oob_params.py` · `oob_parser.py` (7B 페이로드 순수 함수) | ✅ 완료 | pytest |
| 2 | `ble_oob.py` — `BleOobClient`(ABC) + `SimulatorOobClient` | ✅ 완료 | pytest |
| 3 | UI 통합 (연결 방식 세그먼트·스캔·주소 자동 반영·타임라인 실연동) | ✅ 완료 | 시뮬레이터 코드 검증 — **사람 눈 확인 미완** |
| 4 | `BleakOobClient` (실물 BLE, 전용 스레드 asyncio 루프) | ✅ 완료 | 폰 없이 실어댑터 스캔만 확인 — **실폰 미검증** |
| 5 | 안정성 (BT 어댑터 부재·BLE 끊김·재연결) | ⬜ **남음** | — |
| E2E | 사양서 §8 검수 9항목 | ⬜ **남음** (실폰 필요) | — |

**다음 담당자가 할 일 (순서대로)**
1. **실폰 E2E 1차**: 폰 앱(uwb_controlee_app)의 OOB peripheral이 동작하는 상태에서
   `main.py`의 `USE_SIMULATOR = False` → UI에서 `OOB 자동` 선택 → [OOB 스캔] →
   10초 내 주소 자동 반영 확인 (사양서 §8-1). 실패 시 로그의 `[OOB]` 라인부터 볼 것.
2. **5단계 3항목** 구현 (§1의 5단계).
3. **E2E 검수 9항목** 통과 기록 (§1의 E2E 절).

**깨지면 안 되는 원칙**: OOB는 **부가 경로**다. BLE가 없든 실패하든 `수동 입력` 모드는
v1.0_non-oob와 100% 동일하게 동작해야 한다 (수동 모드에서는 BLE 코드가 아예 실행되지 않는다).

**전체 테스트**: `pytest tests/ -v` → 86개 통과 (2026-07-14 기준).

## 1. OOB 2차 — 폰 주소 자동 교환 (최우선)

현재는 폰 앱 화면의 주소를 눈으로 보고 콘솔에 수동 입력한다(1차 방식).
BLE로 자동화하여 "앱 Start → 콘솔 [OOB 스캔] → 주소 자동 수신 → (옵션) 자동 레인징"이 되게 한다.

> 기준 문서: `docs/oob/BLE_OOB_인터페이스_사양서.md` (v0.2, 마스터) ·
> `docs/oob/변경요구_radar_test_console.md` (FR-OOB-0~9). 폰 쪽은 uwb_controlee_app 리포에서 병행.

- [x] **선행**: 폰 앱 수정 가능 확인 + GATT 스키마 합의 → 사양서 v0.2로 확정
      (Service/Characteristic UUID, OOB_INFO 7B 페이로드, 폰=peripheral/PC=central)

### 0단계 — 문서 (선행 커밋)
- [x] 사양서·변경요구서 커밋 + CLAUDE.md OOB 계약/bleak 격리 규칙 + 본 TODO 세분화

### 1단계 — 파서 (폰 불필요)
- [x] `oob_params.py` — UUID·프로토콜 버전·타임아웃 상수 한 곳 (FR-OOB-2)
- [x] `oob_parser.py` — 7B 페이로드 순수 함수 파서 + `tests/test_oob_parser.py`
      (주소 비반전, session_id LE=`2A 00 00 00`, 7B 미만 실패, 추가 바이트 무시, 버전 0x02 경고)

### 2단계 — BLE 추상화 (폰 불필요)
- [x] `ble_oob.py` — `BleOobClient`(ABC): `scan()`/`connect()`/`read_oob()`/`disconnect()` +
      콜백 `on_oob_info`/`on_disconnect`/`on_log` (+ `on_scan_result`/`on_state` — 타임라인 실연동용)
- [x] `SimulatorOobClient` — 가짜 스캔 목록·OOB_INFO·주소 변경 Notify·실패 시나리오
      (BLE_CONN_FAIL, OOB_PARSE) 재현 (FR-OOB-8) + `tests/test_ble_oob.py` 18케이스
      (시뮬 11 + BleakOobClient의 BLE 무관 경로 7)

### 3단계 — UI 통합 (시뮬레이터로 전 흐름 검증, 폰 불필요)
- [x] 연결 방식 세그먼트 `수동 입력`/`OOB 자동` — **기본 수동**, 레인징 중 비활성 (FR-OOB-0)
- [x] [OOB 스캔] 버튼 + 발견 목록 (1대 자동 선택, 다중이면 목록 선택 UI) (FR-OOB-1)
- [x] 주소 입력칸 자동 반영 — OOB 모드에서 읽기전용(🔒) (FR-OOB-3)
- [ ] 다중 폰 **동시 선택** 시 쉼표 조합(multicast §2 연계) — 순차 연결 오케스트레이션 필요,
      현재는 목록에서 1대 선택 (§2 실측과 함께 구현)
- [x] SessionID 교차 검증 — 불일치 시 경고 + "수신값으로 갱신" 선택지 (FR-OOB-4)
- [x] 자동 시작 토글 — ON이면 주소 반영 직후 start_ranging, **기본 OFF** (FR-OOB-5)
- [x] 주소 변경 Notify 처리 — 입력칸 갱신+로그, 레인징 중이면 "재시작 필요" 경고 (FR-OOB-6)
- [x] 세션 타임라인 실연동 — BLE_ADV→BLE_CONN→OOB_DONE 실이벤트 점등, ERR 시 REASON
      표시. 수동 모드는 현행 유지 (FR-OOB-7)
- [x] 시뮬 재현 도구: BLE 실패 재현 드롭다운(BLE_CONN_FAIL/OOB_PARSE/SESSION_MISMATCH)
      + [주소 변경 재현] 버튼 (시뮬 모드 전용 노출)
- [ ] 회귀 확인: 수동 모드 선택 시 현행 화면·동작과 100% 동일 + BLE 코드 미실행
      (코드상 보장 — 사용자 화면 확인 대기)

### 4단계 — 실물 BLE (실폰 필요)
- [x] `requirements.txt`에 bleak 추가 (버전 고정 3.0.2 — 1단계에서 선반영)
- [x] `BleakOobClient` — 전용 스레드 asyncio 루프(Flet 루프와 분리), UUID 필터 스캔,
      연결 타임아웃 10s, Notify 구독, 안전 종료 (FR-OOB-1·2·6 실물)
      — 폰 없이 실어댑터 스캔 확인(0대 발견·비블로킹·좀비 스레드 없음). **실폰 검증 대기**
- [x] 스캔 캐시 대응 — 결과에 RSSI·타임스탬프 표기(목록 라벨+로그), 재스캔은 [OOB 스캔] 재클릭

### 5단계 — 안정성 (← **여기부터 남은 작업**)
- [ ] **BT 어댑터 없음/OFF 처리 (FR-OOB-9)** — 현재는 bleak *미설치*만 막고(`is_bleak_available()`
      → 수동 모드 유지), 어댑터가 없거나 꺼진 경우는 **스캔을 눌러야 실패를 안다**
      (`BleakOobClient._scan_async`가 예외를 잡아 "스캔 실패 + 빈 목록"으로 로그).
      할 일: 어댑터 상태를 미리 조회해 [OOB 스캔] 버튼을 비활성 + 사유 표시.
- [ ] **레인징 중 BLE 끊김 실측** — 코드상으로는 보장됨(`ble_oob.py`는 UCI를 전혀 호출하지 않고,
      `_on_peer_disconnect`는 로그·콜백만). 폰 BLE를 끄고 거리 측정이 계속되는지 확인만 남음 (사양서 §7-7).
- [ ] **재연결** — 현재는 끊기면 사용자가 [OOB 스캔]을 다시 눌러야 한다. 자동 재연결 필요 여부
      판단 후 구현 (브링업 도구이므로 수동 재스캔으로 충분할 수도 있음 — 실폰 사용감 보고 결정).
- [ ] 로그 정리 — `[OOB]` 라인이 스캔 시 기기 수만큼 늘어난다. 실폰에서 과한지 확인.

### E2E 검수 (실기기 — 사양서 §8) (← **남은 작업**)
- [ ] 1. 폰 Start → [OOB 스캔] → **10초 내** 주소 자동 반영
- [ ] 2. 자동 시작 토글 ON → 주소 수신 즉시 레인징 시작 (기본은 OFF)
- [ ] 3. 폰 Stop→재Start(주소 재발급) → Notify로 입력칸 자동 갱신 + 로그
- [ ] 4. 폰 BLE 권한 거부 → 양쪽 모두 기존 수동 흐름으로 레인징 성립
- [ ] 5. 콘솔 SESSION_ID를 43으로 바꾼 뒤 수신 → 불일치 경고 + "수신값으로 갱신" 동작
- [ ] 6. 타임라인이 BLE_ADV→BLE_CONN→OOB_DONE→RANGING 순으로 실제 점등
- [ ] 7. 레인징 중 BLE만 끊어도 거리 측정 지속
- [ ] 8. **수동 모드 회귀 없음** — v1.0_non-oob 태그와 동일 동작
- [ ] 9. 교차 조합(앱=OOB / 콘솔=수동)에서도 레인징 성립

## 2. multicast 실측 검증

콘솔은 one-to-many 세션을 지원하지만(주소 쉼표 입력) 실측은 폰 1대로만 확인됐다.

- [ ] 폰 2대 준비, 각 폰의 주소 확인 후 `5F:DD, A1:B2` 형식으로 입력해 동시 레인징
- [ ] **폰 쪽 프로파일 확인**: 폰 앱이 multicast 설정(CONFIG_MULTICAST_DS_TWR 계열)으로
      시작해야 함 — unicast 프로파일이면 "세션은 붙는데 측정 0건" 무증상 실패
- [ ] 측정 누락/드롭 발생 시 `uci_params.SLOTS_PER_RR`(현재 6) 튜닝 검토

## 3. 진단 도구 강화 (브링업 목적)

- [ ] 거리 시계열 그래프 (외부 차트 라이브러리 금지 — flet.canvas 직접 렌더)
- [ ] 측정 데이터 CSV 내보내기 (타겟별)
- [ ] 타겟별 통계: 평균/표준편차/측정 드롭률

## 4. 소소한 개선

- [ ] CLI JSON 경로 target_id: `parser._parse_json_line()`에서 `Addr` 필드를
      `target_id`로 채우기 (현재 None 고정)
- [ ] 미사용 잔여물 처리 결정: `main.py`의 `TABLE_*`/`TARGET_STATUS_*`/`COL_*` 상수,
      `uci_params.split_dest_macs()` — 테이블 개편 보류로 미사용 (OOB에서 재사용 or 제거)
- [ ] `PANEL_WIDTH=470` 유지 여부 (가변형 행에는 300으로 충분)

## 보류 (사용자 결정)

- 우측 패널의 5행 고정 컬럼형 테이블 개편 — **가변형 행 유지**로 결정 (2026-07-11)

## 완료 기록

- 2026-07-11 `0a5a957` — 다중 타겟 표시(타겟별 행·수신율·색상·링) + UCI multicast +
  실기기 검증 성공 (1차: 주소 수동 입력 방식)
- 2026-07-11 `89bde00` — 타겟(주소)별 거리 표시 기반 작업 + Flet 0.85.3 업그레이드 +
  UCI 벤더 재동기화
- 2026-07-02 `5d4ef18`~`f8bfed2` — 콘솔 1~5단계 구현, CLI JSON 파서, UCI 직접 구동(6단계)
